<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>

  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>

<body>

  <div class="float-right">
    <% if @login_merchant %>
    <%= link_to "Logged in as #{@login_merchant.username}", merchant_path(@login_merchant), class: "button" %>
    <%= link_to "Log Out", logout_path, method: :post, class: "button" %>
    <% else %>
    <%= link_to "Log In with Github", "/auth/github", class: "button float-right" %>
    <% end %>
  </div>

  <section class="row status <%= flash[:status] %>">
    <%if  flash[:status] == :failure %>
    <ul>
      <h3><%= "A problem occurred: #{flash[:message]}" %></h3>
    </ul>
    <% else %>
    <ul>
      <h3><%= flash[:messages] %></h3>
    </ul>
    <% end %>
  </section>

  <main class="row">
    <%= yield %>
  </main>

</body>
</html>
