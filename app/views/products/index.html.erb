<main class="row">

  <header class="align-center">
    <h1><%= @category.name %></h1>
  </header>

  <% @products.each do |product| %>
    <section class="small-12 large-3 columns">
      <div>
        <%= link_to image_tag(product.photo_url), product_path(product), class: "thumbnail small" %>

<<<<<<< HEAD
        <p class="inline">
          <%= product.name %> | <%= number_to_currency product.price %>
        </p>
      </div>

      <div>
        <%= form_for @order_item do |f| %>
          <%= f.hidden_field :product_id, value: product.id %>
          <%= f.number_field :quantity %>
          <%= f.submit "Add to cart" %>
         <% end %>
      </div>
    </section>
  <% end %>

</main>
=======
<% @products.each do |product| %>
 <p><%= product.name %> | <%= number_to_currency product.price %>
   <img src=<%="#{product.photo_url}"%> />
 <%= form_for @order_item do |f| %>
   <%= f.hidden_field :product_id, value: product.id %>
   <%= f.number_field :quantity %>
   <%= f.submit "Add to cart" %>
 <% end %></p>
<% end %>
>>>>>>> 31ce3b2f19ab5b9c59d052e22083a3a09c790b6b
